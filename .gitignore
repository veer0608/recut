.venv/
__pycache__/
*.pyc
.env
out/
.pytest_cache/
*.egg-info/
recut.db
*.db

# Ingested source text, cached so a before and after measurement compares the
# same input. Derived, third-party, and re-fetchable: not ours to check in.
eval/.cache/
